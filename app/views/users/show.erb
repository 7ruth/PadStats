<div class="container" style="top: 10vh; width:100%; position:relative; display: flex; flex-direction:row; flex-wrap:wrap; justify-content:flex-start">

<ul>
<% if @locations %>
<%= render partial: "location", collection: @locations %>
<% else %>
<%= "You haven't searched for anything!" %>
<% end %>
</ul>

</div>
